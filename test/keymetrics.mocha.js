/* eslint-env mocha */

'use strict'

const should = require('should')
const Keymetrics = require('..')

describe('Keymetrics General Tests', () => {
  console.log(process.env)

  it('should test', () => {
    return true
  })
})
